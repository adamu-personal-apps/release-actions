{
  "name": "release-actions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable GitHub Actions for Expo release notifications in Slack",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
