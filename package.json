{
  "name": "release-actions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable GitHub Actions for Expo releases and static-site deployments",
  "scripts": {
    "static-site-artifact": "node scripts/static-site-artifact.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
